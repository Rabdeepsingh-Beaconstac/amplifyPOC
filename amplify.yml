version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "New No PreBuild command"
    build:
      commands:
        - echo "New No Build command"
  artifacts:
    baseDirectory: /dist
    files:
      - index.html
  cache:
    paths:
      - node_modules/**/*