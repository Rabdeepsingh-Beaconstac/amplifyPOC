version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "New No PreBuild command"
    build:
      commands:
        - echo "MAster New No Build command"
  artifacts:
    baseDirectory: /
    files:
      - index.html
  cache:
    paths:
      - node_modules/**/*