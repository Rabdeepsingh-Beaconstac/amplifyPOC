version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No PreBuild command"
    build:
      commands:
        - echo "2 No Build command"
  artifacts:
    baseDirectory: /
    files:
      - index.html
  cache:
    paths:
      - node_modules/**/*