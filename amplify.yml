version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "1 No PreBuild command"
        - echo ${FRONTEND_PASSWORD}
    build:
      commands:
        - echo "2 No Build command"
  artifacts:
    baseDirectory: /
    files:
      - index.html
  cache:
    paths:
      - node_modules/**/*