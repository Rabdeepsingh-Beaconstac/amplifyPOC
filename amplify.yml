version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "Build command is empty"
  artifacts:
    baseDirectory: /
    files:
      - index.html
  cache:
    paths:
      - node_modules/**/*